import { WallModel } from './../models/wall.model';
import { UserModel } from './../models/user.model';

// Note: need to touch every model at least once in the beginning of the app to activate the decorators for each model.

// tslint:disable-next-line: no-unused-expression
UserModel;
// tslint:disable-next-line: no-unused-expression
WallModel;
